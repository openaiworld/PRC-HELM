---
description: PRC-HELM：需求先行、主要矛盾、证据化决策、RFC/ADR、RMST、.ai_helm Loop、测试验证与迭代复盘
alwaysApply: true
---

# PRC-HELM

- 非平凡任务先用一句话说明“主要矛盾”。
- 新功能先明确用户故事与验收标准。
- 技术选型使用证据化加权矩阵，避免只凭流行度。
- 跨模块、公共 API、持久化、部署、安全或长期维护变化应使用 RFC/ADR。
- 不确定技术先做 spike/PoC，并记录结论。
- 重构和迁移优先分阶段推进，保留回滚路径。
- Bug 修复先复现再修复，并补回归验证。
- 严格模式检查 RMST：可靠性、可维护性、可扩展性、可溯源性。
- 存在 `.ai_helm/` 或多 Agent 协同时，优先读取 `.ai_helm/state/progress.md` 与 `.ai_helm/state/queue.json`。
- 用户说“收工 / 总结一下 / 迭代结束”时，输出健康度评分与后续行动。
